<h1><%= @artist.fullname %></h1>





<p>Artist first name: <%= @artist.first_name %></p>
<p>Artist last name: <%= @artist.last_name %></p>
<p>Artist genre: <%= @artist.genre %></p>
<p><%= image_tag @artist.image.thumbnail %></p>
<hr>


<h3><b>Songs</b></h3>

<table class="table table-striped" id="songList">
  <%= render @artist.songs %>
</table>
<hr />
<h4><b>Add a song for this artist?</b></h4>
<button type="button" class="btn btn-secondary"><%= link_to "[New Song]", new_artist_song_path(@artist.id) %></button>

<hr>
<%= link_to "Delete artist", artist_path(@artist.id), method: :delete, data: { confirm: "Are you sure?" } %>
